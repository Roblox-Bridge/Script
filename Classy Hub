print("Classy Hub v1.2.1 Loading...") pcall(function() local Players = game:GetService("Players") local plr = Players.LocalPlayer if not plr:FindFirstChild("Data") then repeat wait() until plr:FindFirstChild("Data") end local ReplicatedStorage = game:GetService("ReplicatedStorage") local WS = game:GetService("Workspace") local TweenService = game:GetService("TweenService") getgenv().ClassyHub = {} getgenv().ClassyHub.AutoFarm = false getgenv().ClassyHub.StopTween = false local Mon = "Brute" local NameQuest = "BuggyQuest1" local LevelQuest = 2 local CFrameMon = CFrame.new(-1140.08, 20, 4322.92) local CurrentTween = nil function topos(cf) local hrp = plr.Character and plr.Character:FindFirstChild("HumanoidRootPart") if not hrp then return end if CurrentTween then pcall(function() CurrentTween:Cancel() end) end -- Simple sky tween local up = CFrame.new(hrp.Position.X, 300, hrp.Position.Z) local t1 = TweenService:Create(hrp, TweenInfo.new(0.7, Enum.EasingStyle.Linear), {CFrame = up}) t1:Play() t1.Completed:Wait() local high = CFrame.new(cf.Position.X, 300, cf.Position.Z) local dist = (Vector3.new(hrp.Position.X,0,hrp.Position.Z) - Vector3.new(cf.Position.X,0,cf.Position.Z)).Magnitude local t2 = TweenService:Create(hrp, TweenInfo.new(dist/350, Enum.EasingStyle.Linear), {CFrame = high}) t2:Play() t2.Completed:Wait() local t3 = TweenService:Create(hrp, TweenInfo.new(0.7, Enum.EasingStyle.Linear), {CFrame = cf + Vector3.new(0,10,0)}) t3:Play() t3.Completed:Wait() end function HasQuest() local ok, r = pcall(function() local q = plr.PlayerGui.Main.Quest return q.Visible and q.Container.QuestTitle.Title.Text ~= "" end) return ok and r end -- UI - SIMPLEST local gui = Instance.new("ScreenGui") gui.Name = "ClassyHub_v1_2_1" gui.Parent = plr.PlayerGui gui.ResetOnSpawn = false local frame = Instance.new("Frame") frame.Size = UDim2.new(0,250,0,100) frame.Position = UDim2.new(0,20,0,180) frame.BackgroundColor3 = Color3.fromRGB(20,20,20) frame.Parent = gui local btn = Instance.new("TextButton") btn.Size = UDim2.new(0,230,0,40) btn.Position = UDim2.new(0,10,0,10) btn.Text = "Auto Farm Brute: OFF (No Quest TP)" btn.Parent = frame btn.BackgroundColor3 = Color3.fromRGB(150,0,0) btn.TextColor3 = Color3.new(1,1,1) local lbl = Instance.new("TextLabel") lbl.Size = UDim2.new(0,230,0,30) lbl.Position = UDim2.new(0,10,0,60) lbl.Text = "Ready - Lv "..plr.Data.Level.Value lbl.BackgroundTransparency = 1 lbl.TextColor3 = Color3.new(1,1,1) lbl.Parent = frame btn.MouseButton1Click:Connect(function() getgenv().ClassyHub.AutoFarm = not getgenv().ClassyHub.AutoFarm btn.Text = getgenv().ClassyHub.AutoFarm and "Auto Farm: ON" or "Auto Farm: OFF" btn.BackgroundColor3 = getgenv().ClassyHub.AutoFarm and Color3.fromRGB(0,150,0) or Color3.fromRGB(150,0,0) end) print("Classy Hub v1.2.1 UI Loaded") spawn(function() while wait(0.5) do if getgenv().ClassyHub.AutoFarm then pcall(function() local hrp = plr.Character.HumanoidRootPart if not HasQuest() then lbl.Text = "Accepting quest in background..." -- NO TP TO QUEST GIVER - Direct accept ReplicatedStorage.Remotes.CommF_:InvokeServer("StartQuest", NameQuest, LevelQuest) wait(0.5) ReplicatedStorage.Remotes.CommF_:InvokeServer("StartQuest", NameQuest, LevelQuest) wait(1) else local target = nil for _,v in pairs(WS.Enemies:GetChildren()) do if v.Name == Mon and v.Humanoid.Health > 0 then target = v break end end if target and target:FindFirstChild("HumanoidRootPart") then lbl.Text = "Killing: "..Mon topos(target.HumanoidRootPart.CFrame * CFrame.new(0,15,0)) for _,v in pairs(WS.Enemies:GetChildren()) do if v.Name == Mon and (v.HumanoidRootPart.Position - hrp.Position).Magnitude < 350 then v.HumanoidRootPart.CFrame = hrp.CFrame * CFrame.new(0,0,3) v.HumanoidRootPart.CanCollide = false end end local tool = plr.Character:FindFirstChildOfClass("Tool") if tool then tool:Activate() end else lbl.Text = "Going to: "..Mon topos(CFrameMon) end end end) end end end) print("Classy Hub v1.2.1 Fully Loaded - No Quest Giver TP") end)
